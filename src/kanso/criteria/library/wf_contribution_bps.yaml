id: wf_contribution_bps
kind: objective
applies:
  objective_mode: absolute
priority: 30
meaningful_when: "The return target itself: mean net return per period in basis points of the capital, at any horizon; a period in cash counts as zero. Chosen by name, never by default."
params: {min_delta: float, k_se: float}
ranges: {min_delta: [0.0, .inf], k_se: [0.5, 3.0]}
impl: kanso.criteria.objectives.wf_contribution_bps
