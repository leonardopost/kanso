id: max_hold
kind: gate
stage: card
required: false
meaningful_when: "Informative when the thesis bounds how long a position may be held, in calendar days: a closed position entry fill to exit fill, an open one to the window's close, an attached one at period ends."
params: {days: int}
ranges: {days: [1, 366]}
impl: kanso.criteria.gates.max_hold
