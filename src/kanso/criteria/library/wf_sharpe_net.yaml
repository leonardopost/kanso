id: wf_sharpe_net
kind: objective
applies:
  objective_mode: absolute
  horizon: {min: 1d}
priority: 20
meaningful_when: "A holding period of a day or more produces enough return periods for a Sharpe to mean something; below that, use a per-trade edge."
params: {min_delta: float, k_se: float}
ranges: {min_delta: [0.0, .inf], k_se: [0.5, 3.0]}
impl: kanso.criteria.objectives.wf_sharpe_net
