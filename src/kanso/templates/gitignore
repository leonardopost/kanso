# kanso workspace — rendered as .gitignore by `kanso init`
.env
state.db
state.db-journal
state.db-wal
state.db-shm
envelope.yaml
runs/
sessions/
strategies/*/impl/*/__pycache__/
hypotheses/*/results.tsv
catalog/.cache/
# Uncomment to keep market data out of git (recommended for anything beyond demos):
# catalog/
