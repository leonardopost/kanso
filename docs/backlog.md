# Operator backlog

Blockers and deferred decisions found during the build. Each entry is something the build
worked around rather than stopped for. Nothing here blocks a later milestone; all of it is
worth an operator's eye.

A closed entry stays, with the claim that closed struck through and what closed it beside
it, because what was found is worth as much to a reader as what remains — and an entry
half closed says which half, so the half still open is not read as done. `CHANGELOG.md`
records the same closures as user-visible changes.

| # | milestone | item | what the build did | what it needs |
|---|---|---|---|---|
| 1 | M0 | ~~macOS 26 runner label unverified~~ **closed**: `macos-26` exists and both Python jobs passed on it in the first CI run. | — | Nothing. |
| 2 | M1 | ~~A hypothesis cannot reach `classified` without `classify`~~ **closed**: `kanso classify` landed in M2 and moves the status itself. | — | Nothing. The override path — edit the three keys, `hyp add` — remains and needs no model. |
| 3 | M1 | ~~The demo hypothesis named its universe by the short key while the catalog files datasets under the qualified id~~ **closed.** The demo names the qualified id in both places and `docs/workspace.md` states the rule. | — | Nothing. A hypothesis names universe ids as `<SYMBOL>.<VENUE>`; that is documented, not outstanding. |
| 5 | M1 | ~~`parity_replay` and `param_plateau` ship as declared catalogue items whose implementations are stubs~~ **closed**: `param_plateau` landed in M3 and `parity_replay` in M4. Every gate the toolbox declares now resolves. | — | Nothing. |
| 6 | M2 | ~~Certification is M3's, so a stalled run cannot be certified where the protocol says it is~~ **closed**: `on_stall` certifies the keep it finds and the verdict moves the hypothesis; the requeue rules are unchanged, except that one the verdict turned `failed` leaves the queue rather than returning to it. | — | Nothing. |
| 7 | M2 | ~~The daemon's monitor loop has nothing to check~~ **closed**: M4 filled the pass. The loop runs `kanso.monitor.run_once` every `[monitor] interval` in its own process; a pass that raises is recorded as `monitor_failed` and the cadence is kept. | — | Nothing. |
| 8 | M2 | ~~`src/kanso/templates/models.yaml` and the demo register point operators at `docs/workspace.md`, which does not exist~~ **closed**: the page exists and documents the register, the `instruments.yaml` field split, `portfolio.yaml`, `envelope.yaml` and the `kanso.toml` keys the six template headers promise. | — | Nothing. |
| 9 | M3 | ~~The build plan put the inbox a milestone later than certification, so a certification failure had nowhere to reach an operator~~ **closed**: M3 built the file, the read state and `kanso inbox [ack ID]`; M4 added the webhook and the `promotable`, `demoted` and `deploy_blocked` kinds. | — | Nothing. |
| 10 | M3 | ~~`kanso research run` spends a frontier call and two engine runs after its last card~~ **closed.** The stall it ends on certifies, which is the behaviour, not a fault. | — | Nothing. The command prints the certificate the stall produced, and `--cards N` stops short of it. |
| 11 | M3 | ~~`parity_replay` is declared a structural invariant every plan must include, but has no implementation~~ **closed**: M4 implemented it, `criteria.pending_required()` is empty and its release gate now asserts rather than skips. | The machinery that withholds a declared-but-unbuilt gate stayed; the tests that prove a plan may not name one withhold a real gate by hand instead. | Nothing. A gate declared before the milestone that builds it needs both halves again. |
| 13 | M4 | ~~`paper_forward` judges a paper window's realised objective against a band measured over the certification window~~ **closed.** The two are estimates of different precision, so a plan that judges a fortnight against a year asks a version behaving exactly as certified to look drifted. | Nothing in the gate — the comparison is the one the plan asked for — and the warning went where the plan is chosen. `kanso cert plan` reports the two spans and how much noisier the shorter one is whenever the paper window the plan implies (`min_duration`, `horizon_mult` × horizon) is under a tenth of the certification window the hypothesis declares, and pins the plan anyway. | Nothing in the code. It is a warning and not a refusal: a short paper window is the operator's to mean. One thing an operator should know rather than discover: **the shipped demo trips its own warning** — `kanso cert plan demo_mr` asks for a 5d paper window against a 145d certification window and prints the line (exit 0, nothing else changed). That is honest about what the demo's plan would do on a wall clock, and resizing `min_duration` in `templates/demo/responses.yaml` to clear the floor is a change to what the demo teaches, so it was left for the operator rather than made as a side effect of the warning. |
| 14 | M4 | The suite takes about thirteen minutes, and has grown with the build: roughly four after M1, ten after M3, thirteen once certification began replaying both code paths and a passing certificate started composing and deploying. It is dominated by real backtests. | Left as is at every step. Each of those runs is the behaviour a milestone added, and a fixture that skipped them would prove none of it. | Watch it. If it reaches the point of discouraging a full run before a commit, mark the end-to-end and certification tests so a local run skips them and CI does not. |
| 15 | M4 | A stage node is a bounded run, not a daemon: `portfolio deploy` builds the node, releases every catalog point the stage has not seen and returns. With replay data and a simulated venue there is no window in which that differs from a long-lived node. With a broker's execution client there is: such a client fills against the price the market shows now, and a bounded replay of the catalog would fill its orders in simulation while the stage record — and the paper and live gates reading it — called the money the broker's. | **M7 made it a refusal rather than a silence.** `deploy` refuses a `clock: wall` execution client (exit 2), naming what a stage node in this version actually is, and `kanso portfolio clients` and `kanso doctor` report the same refusal before an operator runs into it. The refusal is made *after* the approval check, so a real-capital client with no approval still fails first for the approval it is missing. Everything either side of the node is live and tested: the clients, their `capital` and `clock` declarations, every refusal, the credential-leak scan and `promote --live --as NAME` as the only path to real capital. | The long-running stage node, which is the last piece between this build and a broker actually trading. It needs: a node that outlives the command that started it; the broker's execution client and live data client registered in place of the replay client and the simulated venue (`reconciliation=True` with non-zero inflight, open and position check intervals, since the client reads no order stream); venue routing per MIC; the kill switch read from the state store on the node's own timer and a flatten when it turns on; the tape recorded beside each run of a stage and compared against the workspace's declared `feed` when the node comes back, so a stage cannot change series between one run and the next (`docs/adapters.md` names this node as where that check lives; nothing in 0.1.0 records a tape, and the data client's unreached `check_feed`/`provenance` seam was removed with that sentence); and a realised window built from the broker's own fills rather than from the catalog extraction. Entry 22 is the last of those. |
| 16 | M4 | ~~The two code paths do not fill an order larger than one book state identically~~ **closed in M7**: kanso assembles its own `SimulatedExchange` and `BacktestExecClient` from the engine's components, on their own message bus, and the two paths now fill an oversized order identically — measured on the case that produced this entry: 25 @ 10.00 then 475 @ 10.01 on both, where the engine's convenience client filled 25 and dropped the rest. | **The repair this row proposed was wrong, and measuring it is what found the real one.** `use_message_queue=True` is not the fix: with the queue on, a command waits for the next `process`, so the order is matched against the *next* point's book — a bar late, at a price the strategy never saw — and the missing remainder still never arrives. `fee_model=None` is not possible: `SimulatedExchange` declares the argument non-optional, and `BacktestEngine.add_venue` substitutes `MakerTakerFeeModel()` for the `None` kanso hands it, so the research venue was never fee-free by configuration either. The actual cause is that `LiveExecutionEngine.process` **queues** an order event where `ExecutionEngine.process` applies it: on an L1 book the exchange fills the top level and then, *if the order is still open*, slips one increment and fills the remainder in the same call — and "still open" is false in a node because the first fill has not been applied yet. The repair is a relay: kanso's exchange and its execution client get their own `MessageBus` that forwards every endpoint to the node's unchanged except the execution engine's event endpoint, which goes to the synchronous implementation. Nothing else in the node changes, and a broker client beside it still uses the live queue. The queue stays off, which is also what lets a node flatten after its last point. | Nothing. Two things it left behind: cost neutrality still rests on the zero-fee instrument invariant rather than on the venue — both paths now carry the same `MakerTakerFeeModel`, so they cannot drift apart over it, but neither is fee-free by configuration; and `kanso replay parity` no longer explains a divergence, because the one cause it knew is the one that was fixed. |
| 17 | M4 | ~~Five tests recomputed a timestamp the product had already taken~~ **closed.** One failed CI at 00:13 UTC, having resolved an instrument either side of midnight and compared the two checksums. | All five fixed according to what each was claiming: pin the input, freeze the clock, or accept either day the call spanned. | Nothing here. The rule now lives in `AGENTS.md`, where a test author meets it. |
| 18 | M5 | ~~Eight defects reached a green suite because every fixture encoded what an author expected rather than what the vendor does~~ **closed.** 3,492 tests at 100% line coverage while the adapter asked the wrong endpoint about option keys, requested a listing under a prefix that 404s, sent a page four times too wide, forwarded a signed `Host`, and read an empty page as an entitlement refusal. | All eight fixed against the live API, each with the test that would have caught it, and each verified to bite by reverting it in place. | Nothing here. The rule now lives in `AGENTS.md`, where an adapter author meets it. |
| 19 | M5 | ~~`kanso data sync` with no `--dataset` extends *every* held manifest~~ **closed.** After a chunked backfill it reached for an interior chunk and was refused for overlapping the chunk behind it — outright once a snapshot pinned that one — so the command could not succeed on any series with history behind it. | `sync` now continues one dataset per series: the newest, which is the only one a successor can follow. `--dataset` still names one directly, newest or not, which is how the dataset in front of a hole is extended on purpose. The M5 acceptance sequence names none, and `docs/cli.md` states both halves. | Nothing. |
| 20 | M5 | The vendor's daily aggregation window is anchored to midnight in the exchange's own zone — measured as ET for `us_stocks_sip`, 05:00Z in winter and 04:00Z in summer — and both transports now add one resolution step to whatever anchor the vendor wrote, consulting no zone at all. | Nothing. Sharing the rule is what makes the two paths agree, and reading no anchor is what makes it correct for a class anchored somewhere else. | Only `us_stocks_sip` has been measured. Before daily bars are trusted for forex or another non-ET class, measure that class's anchor rather than deriving it — deriving a vendor fact is what produced the defect this entry sits under. |
| 21 | M6 | **Deferred out of 0.1.0 by the operator.** SEC EDGAR refuses this build host's command-line egress: every shell request returns `403` with SEC's "Request Rate Threshold Exceeded" page, identically with a declared User-Agent and with none, and it does not clear on a cool-down — an address-level block rather than the documented fair-access rule. The same URLs load normally in a browser on the same machine, and every other host in this build answers the shell fine. FINRA is reachable and was measured in full; it is deferred with EDGAR to keep the pair together. | Both adapters are out of 0.1.0, which ships two first-party adapters — one data vendor and one broker — and reaches everything else through file exports, the synthetic loader or an adapter an operator writes. The measurements are kept here so the work does not start from documentation: **EDGAR** — `acceptanceDateTime` in `/submissions/CIK##########.json` is the only true publication instant (`2026-09-03T22:30:44.000Z` against a `reportDate` of `2026-09-01`), `recent` caps at 1000 rows and older filings paginate through `filings.files[]`; company facts carry `accn`, `filed`, `start`, `end`, `val` and **no instant at all**, so they must be joined on the accession; restatements are the norm, not the exception — 12,680 same-period pairs in Apple alone, e.g. `AccountsPayableCurrent` for `end=2008-09-27` filed under both a 10-K and a later 10-K/A; `frames` rows carry `accn`, `cik`, `entityName`, `loc`, `end`, `val` and no `filed`, which is the measured reason they cannot be stamped; unknown and malformed CIKs both answer `404`, so `403` means only the block; bulk is `companyfacts.zip` 1.41 GB and `submissions.zip` 1.56 GB. **FINRA** — the daily file is `cdn.finra.org/equity/regsho/daily/CNMSshvol{YYYYMMDD}.txt`, pipe-delimited, header `Date\|Symbol\|ShortVolume\|ShortExemptVolume\|TotalVolume\|Market`, volumes **fractional** (`431541.387127`), `Market` a comma-separated venue set, and the last line a bare row count (measured 12266 against 12268 total lines) — that trailer is the load check; a day with no file answers **403, not 404**, so a weekend must not read as a refusal; the `api.finra.org` endpoints return **CSV, not JSON**, and consolidated short interest carries `settlementDate` with no publication instant, which is why the dissemination calendar has to ship as package data. | Whoever builds these needs a network SEC does not block, and SEC's fair-access policy wants a contact address in the User-Agent — the operator's to supply, never kanso's to invent, so the adapter must refuse rather than send a placeholder. Drive `data load`, `data backfill` and `doctor` against both sources before trusting a card built on them. |
| 22 | M7 | **`fill_quality_drift` has no measurement channel.** `StageRecord.realised_slippage_bps` is never assigned by anything in the tree — `monitor/run.py::_record` leaves it at its `None` default — so the gate skips on every stage, honestly ("the execution client reported no realised slippage to compare") but forever. It cannot be fixed inside the gate: a `criteria.run.Fill` carries the *modelled* cost, which the runner's extraction applied, and no reference price is recorded anywhere in a `CardRun`, so no arithmetic on a run recovers what a fill gave up. | The gate was made honest for the clients that now exist: `funding` of `unknown` skips rather than judging (an adapter that failed to import degrades to exactly that, and would otherwise have been judged as if it were the broker); a non-finite realised figure skips rather than returning a `False` verdict that would demote a live version on a broken measurement; and the evidence records which client's fills were judged. `broker_paper` and `real` are both measured, paper deliberately, since that is where fill quality should be learned. | Four files, none of them an adapter's alone: the broker's execution client records the reference price at submission beside the printed average fill price; `nautilus/node.py::Realised` carries the notional-weighted signed slippage in basis points for the window; `portfolio/records.py` and `encode_run`/`decode_run` persist it and `monitor/realised.py::Tenure` combines it across windows (notional-weighted, never a mean of means); `monitor/run.py::_record` passes it. Until then the gate has its arithmetic and not its wiring, and its own docstring says so where a reader of the gate will meet it. |
| 23 | M7 | The execution client reads fills by **polling** the broker's REST endpoints; it opens no order stream. The stream endpoint and its frame shapes were never measured, and a parser built on the published documentation would be a fixture encoding expectation rather than measurement — which is entry 18 in a different costume. ~~The cadence is not the operator's either: a constructor argument with a 15-second default and no `[adapters.alpaca]` key behind it.~~ **That half is closed.** | Polling, with pagination that walks by instant and dedupes by row id, and refuses rather than truncates a page that holds nothing new. The deterministic trade id is what makes a re-read harmless: the same fill read twice derives the same id twice and reconciliation skips it. `poll_interval_s` is now an `[adapters.alpaca]` key, bounded 1 s to 3600 s and still defaulting to 15, refused when the table is read rather than at the first sweep; the constructor argument that answered the same question is gone, so the workspace answers it once. `docs/adapters.md` states the range and the arithmetic that makes it matter — the cadence and the quota together bound how many series a stage may subscribe. | Measure the order stream — connect, subscribe, record the frames — before writing anything that parses one. Two corrections to this entry's own wording, made while closing the settable half: `poll_interval_s` is the **data** client's sweep cadence, not the execution client's, so it is a bar that arrives one sweep late and not a fill. The execution client has no interval of its own — it answers report requests, and how often it is asked is the engine's `open_check_interval_secs` / `position_check_interval_secs`, which `nautilus/session.py` sets neither of because today's stage node is a bounded replay. That knob belongs with entry 15. And `doctor` validates no known adapter's table at all, so a `poll_interval_s` outside its bounds — like a bad `feed` or `requests_per_minute` — is still met only when a client is built. |
| 24 | M7 | `AlpacaBroker.transport(ws)` builds a fresh rate-limited HTTP client per call, and the execution client factory, the data client factory and the tradability overlay each call it. Three clients would be three quotas under one account, which is three times the published limit. | Nothing, because nothing in this build constructs more than one of the three in a process: the stage node that would is entry 15. The single-connection rule is stated in every docstring that matters and each component accepts an injected transport, so passing one shared object is all it takes. | Memoise the transport per workspace — or build it once where the node is assembled and hand it to all three — at the same time as entry 15, and cover it with a test that counts the clients built. |
| 25 | M7 | A resolved venue model records the origin of a *block*, not of a field: `_merge_costs` stamps the whole `costs` block with the last layer that stated anything in it. The broker declares only `commission_bps`, so a card now reports its slippage and spread as having come from the broker when they are kanso's shipped defaults. | Nothing: the numbers are right and only the attribution is coarse. Recorded in the adapter's venue docstring where a reader of that declaration will meet it. | Per-field origins in `schemas/venue.py`, which changes `Origins` from three fields to one per cost field and touches every card, certificate and strategy version that records one. Worth doing before a second broker declares a partial cost model, and not before. |
| 26 | M8 | ~~A gate or an objective declared in `kanso_ext/` is registered nowhere~~ **half closed: the declaration is now refused.** `PROVIDES` accepted both kinds and discovery collected them, while `criteria/library.py` globs `src/kanso/criteria/library/*.yaml` inside the package and `catalogue()`, `plannable()`, `gates()`, `objectives()` and `criteria_version()` take no workspace at all — so the toolbox a plan is drawn from and judged by is always the package's own, and from the outside it read as `kanso hyp validate` refusing `constraints.<id>: is not a gate in the toolbox` while `kanso doctor` was green and listed the extension as loaded. | `ext.REFUSED` holds `gates` and `objectives`, and `_read_provides` refuses either where it is declared with one line naming `docs/extensions.md` — so the schema no longer accepts a declaration that can never work, and the two kinds are gone from `kanso ext show`'s registries and from `doctor`'s shadow check because nothing collects them. An extension whose declaration is refused loses its constructs and execution clients with it, as any unreadable declaration does, and keeps its loaders. `docs/extensions.md`, `docs/workspace.md`, `docs/maintainers.md` §3 and the `kanso-upstream` skill all say a criterion is written in the package. | The merge itself, if a workspace toolbox is ever wanted: give `_catalogue`, `catalogue`, `plannable`, `gates`, `objectives` and `criteria_version` a workspace under the packaged-id-wins rule. `criteria_version` is the hard half and the reason this half was not built — it must cover the merged set, or two workspaces with different toolboxes stamp the same criteria version on a certificate and the pin stops meaning anything. That is a release-sized decision rather than a fix. |
| 27 | M8 | ~~`doctor`'s shadow check sees three kinds out of seven~~ **closed.** `builtin_ids()` read the loader, data-adapter and *broker* execution-client registries, so an extension shadowing a construct, a custom data type or the framework's own `sandbox` execution client was graded `ok` with nothing said. | The shadow check reads one registry per kind `PROVIDES` accepts, and it is now the same table `kanso ext show` compares against — `kanso.ext.shipped(ws)`: the built-in loaders and every loader a packaged adapter provides for the workspace, the packaged adapters, the packaged construct ids, `BUILTIN_TYPES`, and `clients.builtin()`, which is `sandbox` beside each broker's. A test asserts its kinds are exactly `ext.KINDS`, so a kind added to the declaration and not to the table is caught rather than graded green, and another that an extension declaring `massive_bars` is `shadowed` in `ext show` and `warn` in `doctor` alike. Gates and objectives are not among them: entry 26 refuses those declarations, so there is nothing left to shadow. The remedy line was already corrected — every registry (`loaders`, `registry.adapters`, `clients.registry`, `construct._merge`) keeps the packaged one, so the extension's is used nowhere. | The loader residual is closed: the table takes the workspace, so an adapter's loaders are in it, and `docs/extensions.md` says both commands see the same ids. One residual stays: the two commands disagree on what "loaded" means — `doctor` counts an extension whose `PROVIDES` did not read as *not* loaded (`0/1 loaded`) where `ext show` says `1/1 loaded · 0 registered`, because one is counting usable declarations and the other imported modules. Pre-existing for an unusable kind and reachable through entry 26's refusal as well. Pick one meaning and say it in both. |
| 28 | M8 | **A parquet deleted by hand is invisible until a run needs it.** The manifests are the record, so `kanso data show` keeps reporting the dataset, its span and its row count, and `kanso doctor` has no catalog check at all. The loss surfaces at the next baseline or card as `data: the catalog holds nothing for <id> over <window>` (exit 2) — ~~wrapped, for a baseline, by a refusal whose remedy is `fix hypotheses/<id>/strategy.py and begin again`, which is the wrong remedy for a data fault.~~ **That second half is closed; the invisibility is not.** | **The remedy a baseline reports is now the failure's own**, where it was `fix hypotheses/<id>/strategy.py and begin again` for every cause alike. The card's child process sends back the remedy of a `KansoError` it raised beside the traceback tail, and the baseline refusal carries it: missing rows send an operator to `kanso data load`, and a baseline that failed for a reason naming no remedy — a strategy that raised — still says to fix `strategy.py`. `docs/workspace.md` quotes the refusal as it now reads. | A `catalog` check in `doctor`, comparing the manifests against the files, which would find the loss before a run does. |
| 29 | M8 | ~~An edited file kanso owns is caught under `impl/`, not under `hypotheses/`~~ **closed, in two halves.** ~~Appending a line to a file under `strategies/<id>/impl/<version>/` was worse than editing anything else: the next `kanso portfolio deploy` ran the edit and reported success.~~ ~~Appending a line to `hypotheses/<id>/strategy.py` still leaves `doctor` green, is mentioned by no command, and is overwritten without comment by the next keep.~~ Both were provoked, not reasoned about. | The digest check is where the version is used: `strategy/impl.py` hashes every source against the `strategy_sha` its manifest records on both ways out of the directory — loading a version into a node, and reading its sources for a replay — so `portfolio deploy` and `replay run --strategy` refuse an edited, truncated or deleted file by name (exit 3), naming both digests and the certified copy beside the certificate to restore it from. The `hypotheses/` half is documented in `docs/workspace.md` as behaviour, with the `strategy_sha` that makes the divergence findable one `shasum -a 256` away. **Closed:** `doctor`'s `best` check warns when `hypotheses/<id>/strategy.py` differs from or lacks the best blob, naming both shas and the two ways out (`research begin --from-workspace`, or `research show <id>` redirected over the file); it never fails, since editing that file is exactly how an operator prepares `research begin --from-workspace`. | Nothing. |
| 30 | M8 | ~~`[data] reference` demands its credential even when nothing needs resolving~~ **closed.** `resolve_universe` built the configured reference provider before it looked at whether any id was still unresolved, so a workspace whose entries are all `manual: true` — the demo, and every file-export workspace — could not name a reference adapter without setting that adapter's key. | The provider is built only once an id is left unresolved, which is the moment the credential is actually wanted. A CLI test resolves a wholly manual universe under a named reference adapter with none of its variables set, and `docs/workspace.md` says under `instruments.yaml` that naming a vendor is not needing one. | Nothing. |
| 31 | M8 | ~~`portfolio show` reads the file where `deploy` reads state~~ **closed.** A hand-added entry under `stages.live.strategies` printed as a deployed version holding capital while `kanso portfolio deploy --stage live` admitted `0 version(s)`. | The safety is untouched — a file entry funds nothing and approves nothing — and the display now reads the file against the record: an entry no deployment wrote prints as `not deployed · in portfolio.yaml only`, carries `"recorded": false` under `--json`, and is counted in neither the stage's allocation nor its P&L nor its liveness. | Nothing. |
| 32 | M8 | ~~The state-schema guard has two holes~~ **closed in M8.** A database *ahead* of the package — an operator who downgraded — was undetected, because `pending()` only looks upward, so `doctor` called the schema up to date while every command ran against a shape this kanso does not know. And `python -m kanso.research serve`, which the service recipe documents as a unit's `ExecStart`, opened the store directly and skipped the command layer's refusal. | One guard, `state.usable()`, refuses a database in either direction and is called by both the command layer and the daemon entry point — the latter *before* the supervisor takes its lock or spawns a child, because the supervisor never opens a store itself and would otherwise bring a unit up and restart lanes that each refuse, showing the operator a crash loop rather than the one sentence that explains it. `StateStore.ahead_by()` is the half `pending()` could not see. | Nothing. Downgrading across a migration is still unsupported and `docs/maintainers.md` still says so; what changed is that trying it is now refused by name instead of silently permitted. |
| 33 | M8 | ~~`construct.params` are not validated where the rest of the construct is~~ **closed.** `hyp validate` and `hyp add` accepted a construct parameter outside its declared set — `_check_construct` checked the host and the objective mode and never reached the params check — so the refusal arrived where the harness is built, which for a fresh run is inside the baseline card. | `_check_construct` asks the construct itself, through the same `check_params` the harness calls, so the two refuse the same classification in the same words; the call joined the `Construct` protocol, because it is now part of what validation asks of every construct. `docs/constructs.md` quotes the refusal from where it now arrives. | Nothing. |
| 34 | M8 | ~~`portfolio` is both the host sentinel and a legal strategy id~~ **closed.** A sleeve named `portfolio` composed a strategy of that name, and an overlay legitimately hosted on it was then refused with the portfolio-level seam message. | The word is reserved as a hypothesis id rather than respelled as a sentinel: `hyp validate` and `hyp add` refuse it with one refusal saying why, so no sleeve can compose a strategy `construct.host` could not tell from the book. `docs/constructs.md` states the rule under `allocation` and `docs/workspace.md` where an operator names a hypothesis. | Nothing, with one edge left as it is: `hyp new portfolio` still scaffolds the directory, since the word is reserved at validation and `hyp add` is what refuses it. |
| 35 | M8 | ~~`doctor` is the one command that cannot see a state database *ahead* of the package~~ **closed.** `_schema` asked `pending()`, which only looks upward, so a `--demo` workspace stamped `PRAGMA user_version = 99` reported `ok schema 99 · up to date` and `13 ok · 0 fail`, while `kanso hyp show` beside it exited 2 naming the same database as written by a later kanso. | `_schema` asks `ahead_by()` as well and grades a database ahead `fail`, with the remedy the command layer already gives; `doctor` exits 2 on it like every other command. The omission was in the entry-32 fix, which routed the command layer and the daemon through one guard and left `doctor`'s own check reading `pending()` directly. | Nothing. `docs/maintainers.md` §4 said an ahead database "is not detected" and that commands "run against a schema this kanso does not know" — both made false by entry 32 and this row, and both replaced with what now happens. |
| 36 | M8 | ~~`envelope.yaml` was committed, so a clone graded a foreign host as its own~~ **closed.** The workspace template `.gitignore` did not exclude `envelope.yaml`, a host measurement headed "do not edit", so it travelled in the repository and a clone's `doctor` reported another machine's cores, memory and lane plan as this one's. | The template now gitignores `envelope.yaml`; `init` still writes it but it is not committed, so each host detects its own — a missing envelope is a `doctor` warn whose remedy is `kanso env detect`, the re-detect path rather than the reported-foreign one. | Nothing. |
| 37 | M8 | ~~A clone re-certified the same bytes and laundered the trial count~~ **closed.** The immutability refusal consulted only `state.db`, which a fresh clone lacks, so `cert run` minted `f729a53-1-p1-e1.231.0.yaml` beside the committed `f729a53-4-…` — same bytes, plan and engine, the trial count reset from 4 to 1. | `cert run` now also refuses a certificate already on disk for the same subject, plan and engine, matching by name with the trial count wildcarded and naming the file it found, so a clone is refused by the files it has whatever the record says. | Nothing. |
| 38 | M8 | ~~A committed, certified version could not be replayed until its hypothesis was re-registered~~ **closed.** `kanso replay parity --strategy demo_mr@2` exited 2 (`'demo_mr' is not a registered hypothesis`) in a clone, though `kanso strat show` answered from the file. | A strategy target resolves its hypothesis from the pinned bytes when the registry holds them and from the committed `hypotheses/<id>/hypothesis.yaml` otherwise, so a committed version replays from its files. A version does not carry its own traded universe or forward window — those live in the hypothesis, which is why the hypothesis file is read rather than the strategy file and its impl alone. | Nothing. |
| 39 | M8 | ~~`deploy` and `monitor` called a version deployed while `portfolio show` called the stage down~~ **closed.** In a clone `strategy_versions` is empty, but `deploy` admitted candidates from `strategies/<id>/strategy.yaml`, ran a node and recorded a window, while `portfolio show` (entry 31) read the empty record and reported the stage down; the monitor then judged the window `deploy` had recorded. | `deploy` now admits only versions the state record knows — the same record `portfolio show` reads — so a version the record forgot is admitted by neither and no node runs for it, and the monitor finds no window to judge. This makes both directions of entry 31 consistent rather than adding a third rule. | Nothing. |
| 40 | M8 | ~~A researched hypothesis reclassified onto another construct kept its `best`~~ **closed.** `hyp add` cleared `best` on a change of `universe`, `resolution` or `data_requirements` only, and `classify` compared the new construct against a registry column the strip re-pin had just nulled, so the one CLI route back to a classifiable status — strip the three keys, `hyp add`, `classify` — carried a sleeve's champion into a filter, and `research begin` then took that blob into a modifier harness with a remedy (`fix strategy.py and begin again`) that `begin` could not honour, since it takes the best blob again. Provoked, not reasoned about. | `construct.id` joined the scope a `best` is comparable under, in the registry's re-pin, which every route goes through — a hand-edited `hyp add`, the strip, `classify` itself — so `classify` no longer clears on its own; `best_cleared` names the field that moved and fires only when a best existed. A baseline that fails when the run started from `best` names `kanso research begin <id> --from-workspace`. | Nothing. Stripping the classification clears `best` too, by design: a draft has no construct. |
| 41 | M8 | ~~`doctor` ran none of the four record checks its contract listed~~ **closed.** Workspace `strategy.py` vs `best`, certificates and versions whose `strategy_sha` has neither blob nor `.py`, the instrument registry (universes resolvable, overrides and manual entries listed, drift against the newest snapshot) and lane directory health were all absent, while the packaged `kanso-data` skill asserted the instrument one existed. | Added as `best`, `certificates`, `instruments` and `lanes`, graded by what each fault stops: a subject whose bytes are held nowhere and an open run without a directory fail; an edited best-so-far, a lane note, an orphaned lane directory, an id only a vendor could re-resolve and a store the newest snapshot no longer describes warn. The drift `instruments` reports is the comparison `research begin` pins a run by, asked of the package rather than remade. `doctor` still writes nothing (an absent `state.db` is never created, `catalog/` never made) and calls no vendor. | Nothing. |
| 42 | M8 | ~~The instrument store was append-by-date, and a same-dated correction was silently dropped~~ **closed.** kanso stamps a definition's `ts_init` with the date it was resolved as of, and NautilusTrader 1.231.0's catalog skips a write whose file already exists, saying so only on stdout, so `kanso data instruments resolve --refresh` after an `override` edit exited 0, printed the corrected definition and its checksum, and left the store holding the old one; `instruments.yaml` then recorded a checksum the store did not hold, and `data instruments show <ID>` listed every dated copy. Provoked, not reasoned about. | The store is the registry of record and a date holds one definition: a same-dated definition that differs is a correction, refused by name (exit 2) by the plain command and made by `--refresh`, which removes the held file before writing; every write is read back, and a definition the engine skipped is an error rather than a printed line; a definition dated otherwise is added beside the held ones, so a run pinned to an earlier date still reproduces. `show <ID>` renders the one definition a run would use, the newest-dated. `hyp validate` and `hyp add` resolve without recording, so `data instruments resolve` alone writes the store and the cache. | Nothing. |
| 43 | M8 | ~~A snapshot's instrument checksum was written and never read back~~ **closed.** `covering` chose a snapshot on its datasets alone, so `research begin` pinned a run to a snapshot whose instruments the store no longer held, silently, and `doctor` reported every check ok; the documented first-run order (`data load`, `data snapshot`, `hyp add`) froze the checksum of an empty store, because `hyp validate` was what first wrote a definition, and the demo's certificate and both strategy versions cited that vacuous pin. Provoked, not reasoned about. | `research begin` hands out the newest covering snapshot whose instrument checksum is the store's own, refuses by name — the snapshot, what it pins, what the store holds — when the definitions moved since the newest covering one, and refuses a universe id the store holds no definition for; `data snapshot` refuses to freeze an empty store over instrument data; a run resolves its universe for the venue model without recording, so beginning a run or running a card never moves the store under the snapshot it was just pinned to. The README, `docs/concepts.md` and the `kanso-data` skill put `data instruments resolve --as-of <research start>` before `data snapshot`. | Nothing. Resolve as of the research start; a definition dated otherwise is held beside it, a snapshot pins both, and `doctor`'s `instruments` check names any drift. |
| 44 | M8 | A lane writes no log of its own, and no file under `runs/` records what a run did; the design once promised a `runs/<lane>/<hyp>-<tag>.jsonl` per run, left in place by `research end`. | The record of a run is the card rows and the `events` table in `state.db`, read back with `kanso research show`, `research status`, `results.tsv` and `status`; the daemon and its lanes share one plain `runs/daemon.log`. `docs/concepts.md` and `docs/workspace.md` say so. | Nothing, unless a per-run file that travels without `state.db` is wanted: a JSON-lines rendering of the run's events beside the lane directory, written on every event and left by `research end`. |
| 45 | M8 | No `tests/live/` tree: no credentialed smoke test exists for the two shipped adapters, and the `live` marker and `-m 'not live'` deselection declared for one selected nothing. | The marker and the deselection are removed; credentialed acceptance is a maintainer-driven CLI run against the live vendor or the paper account, recorded in the pull request body (`docs/maintainers.md` §2; the M5 and M7 commit bodies are the precedent). | A `tests/live/` tree marked `live`, deselected by default and excluded from the coverage gate and pull-request CI, if a repeatable credentialed smoke per adapter is ever wanted; only a maintainer with the keys could run it. |
| 46 | M1 | `hyp validate` refuses a universe spanning more than one **account** currency, keyed on each venue's model, and never compares an instrument's own quote currency with the account funding it: a EUR-quoted instrument on a USD venue validates, its leg fills in EUR, and the engine's `insufficient data for EUR/USD` error is discarded because kanso builds engines with logging bypassed. | The `kanso.toml` template comment and `docs/workspace.md` state the check as it is; every shipped venue defaults to USD, so the refusal is unreachable without a `venues.<MIC>.currency` override. | Compare each resolved instrument's `currency` with its venue model's in `venue_models()` (both objects are in hand) and refuse the mismatch at `hyp validate` (exit 3), with a test that puts a EUR instrument on a USD venue. |
| 47 | M7 | A stage's `speed` paces nothing: `deploy` builds the `StageNode` without a `pace`, so the bounded catch-up replays unpaced while the value is validated, recorded on the session and printed by `portfolio show` — a stage at `speed: 1` replays three months of minute bars in seconds. | `docs/cli.md`, `docs/workspace.md` and the `portfolio.yaml` template say the value paces nothing in this version and that only `replay run --speed` paces. Entry 15's "no window in which that differs from a long-lived node" is false in this one dimension. | Hand `configured.speed` to `StageNode(pace=…)` when the long-lived node lands (entry 15), with a test that measures the sleeps requested; or drop the key from the stage until then. |
| 48 | M2 | Every tier must have a model, checked where a call is about to be made — `classify`, `cert plan`, `research run`, `align check`, `models check` — and not at `research begin`, which was once named as a check point. | Deliberate: `begin` opens no register and a run begun by hand is measurement only, so it completes with `models.yaml` moved away (`docs/cli.md`, `docs/workspace.md`; pinned by `tests/cli/test_research.py::test_begin_needs_no_model_register`). The consequence: `research run` under a register missing a tier spends the baseline, creates the lane and moves the hypothesis to `researching` before the first `propose` refuses, leaving an active run behind. | `research run` should check tier coverage for `propose` before it begins a run, so a register that cannot serve the loop refuses before the baseline is spent. |
| 49 | M0 | `kanso.toml`'s `schema_version` (and `kanso_version`) are written by `init` and read by nothing: the version `migrate` advances and `doctor` guards is `PRAGMA user_version` in `state.db`, and the template's literal `1` was never moved when the second migration landed. | `docs/workspace.md`, `docs/maintainers.md` and the release skill say the key is not the guard and nothing is bumped by hand. | Either default `Config.schema_version` and drop the key from the template (a required field with no default would break every existing `kanso.toml`), or have `doctor` compare it to `user_version` and say so. |
| 50 | M5 | `InstrumentProvider` and `ResolveError` live at `kanso.data.instruments` and are not re-exported from `kanso.data`, unlike `Loader`, `Adapter` and `register_custom_type`. | The docs and the adapters package docstring name the real path. | Re-export both from `kanso/data/__init__.py` (there is no import cycle), so the provider sits beside the other stable interfaces. |
| 51 | M1 | A hypothesis without `quote` in `data_requirements` is refused at `hyp validate` unless it or `venues.<MIC>.costs` sets `fixed_bps`: no default width exists and the shipped broker declaration supplies a commission and no spread, while the scaffold, the skills and the docs said costs could be left alone. | The scaffold comment, `docs/workspace.md`, its refusal table, the `portfolio.yaml` template and the `kanso-hypothesis`/`kanso-promote` skills now say what a bar-only hypothesis must set. | Nothing: a default width would be a numeric opinion the package does not hold. Revisit if a broker declaration starts supplying a spread. |
| 52 | M5 | `data backfill` and `data sync` fetch through the loader the spec names and never prefer an adapter's bulk transport, although the Massive adapter declares one and `Capabilities.bulk` is read by nothing outside the adapter's own listing. | Deliberate: the ids never collide, so a spec picks a transport by naming one and `sync` continues a series over whichever transport wrote its newest dataset (`docs/adapters.md`, `docs/cli.md`). | An automatic preference needs a rule for when the bulk path's basis (unadjusted day objects) matches the spec's; until one exists the choice stays the operator's. |
| 53 | M2 | The daemon reads `envelope.yaml` as last detected and does not re-detect at startup; detection runs at `init` and `env detect` only. | Deliberate: a plan that changed under a running fleet would change the lane count mid-run; `doctor` flags an envelope whose host changed, and `docs/cli.md` and `docs/maintainers.md` say so. | Re-detect in the supervisor's `serve` entry point, writing through `env.write`, if a service-managed host is expected to change hardware between restarts. |
| 54 | M2 | An `overlay` with `host: portfolio` is refused as an unknown host — at `hyp validate`/`hyp add` (exit 3, "not a certified strategy") and by `classify` (a model answer naming it is rejected) — rather than accepted by classification and refused at `research begin` with the seam named, as `allocation` is; the overlay's `portfolio_seam` string is reachable only by calling `harness()` directly. | `docs/constructs.md` states the refusal in the table and under `allocation`. The catalogue item declares `needs_host: sleeve`, and `needs_host` is single-valued, which is why the build landed here. | Either a multi-valued `needs_host` (the `NeedsHost` literal and the item schema) so the portfolio host is classifiable and refused at `begin` with the seam, or correct the overlay docstring's "classifiable and refused" to "refused at registration" (`src/kanso/classify/constructs/overlay.py`). |
| 55 | M8 | ~~A clone of a research repository reproduces the files but not the record, by design~~ **decided.** `state.db` is gitignored, so a fresh clone has no research history, no `best` pointer, no certificate of record, no trial count, no approvals, no strategy-version index, no session index and no inbox read state, while every file reproduces to the digit. | The operator chose the second way out: **a clone is a fresh workspace that inherits certified work**, documented as such in `docs/workspace.md`, and `doctor` gained a `record` check that names the situation once — certified files on disk the record has no memory of — as a warning, with what did not travel and what re-establishes it. Approvals do not travel on purpose: real capital needs a person to say so again on the machine that will trade. A portable, content-addressed export of the record was declined: multi-host is a stated non-goal of 0.1.0, and it would raise whose record wins when two clones diverge. | Nothing now. Revisit an export only if research is genuinely continued across machines, and decide the merge rule first. |
| 56 | M5 | `kanso data adapters` and `kanso doctor` disagree about an `[adapters.<id>]` table for a broker: with `[adapters.alpaca]` in `kanso.toml`, `data adapters` notes that nothing here provides it while `doctor` consults the broker registry and grades adapters ok. `_adapter_notes` (`data/commands.py`) and `_unprovided` (`cli/doctor.py`) are two copies of one rule that diverged; `check_adapters` and doctor's `_adapters`, and the two row renderers, are the same duplication. | Reproduced on a fresh workspace during the mechanical audit. `data adapters` walks only the data registry and `docs/cli.md` now says so; the divergence itself is unchanged. | One rule read by both: either `doctor` calls `commands.adapters`/`check_adapters`, or the unprovided check moves to `data/registry` with both callers reading it. Closing it by having `data/commands.py` import `kanso.nautilus.adapters` is a new import edge the isolation tests must accept before choosing the direction. |
| 57 | M4 | **A card's memory cap bounds the child; the parent that reads and pickles the whole window is unbounded, and the parent is a daemon lane.** `window_data` reads the research window in the parent and `run_subprocess` pickles the points to hand to the isolated child, while `_watch` polls only the child's resident size. Measured on one session of one instrument's trades and quotes (829,254 trades, 943,478 quotes): reading costs 0.56 GiB and `pickle.dumps` of the two groups peaks at **1.80 GiB** — three times the data — for a blob of 0.146 GiB. Two instruments over one session is about 3.6 GiB in the parent before the child's own copy, so on a 16 GiB host a tick-granularity research window is two to four sessions. Disk is not the constraint; the transient is. | Nothing yet. Found while sizing a microstructure hypothesis against a real vendor, not by reading. A lane that exceeds the host's memory is killed by the operating system rather than reported as a card that was too expensive, so the failure arrives as a dead daemon rather than a crash card with a reason. | Bound the parent the way the child is bounded, and stream rather than materialise: `pickle.dump` to the child's file incrementally instead of building one bytes object, or hand the child the pinned dataset ids and let it read the catalog under a read-only path rather than serialising the points — which is a larger change, because the child having no catalog path is what makes the embargo a refusal rather than a rule. Measure the cap against `[research] mem_cap_gb` so one number governs both processes. |
